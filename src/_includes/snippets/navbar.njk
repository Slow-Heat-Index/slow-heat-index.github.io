<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="/img/logo.png" width="30px" alt="Home">
            Slow Heat Index
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto ms-auto">
                {% for entry in collections.all | eleventyNavigation %}
                    <li class="nav-item">
                        {% if entry.url == page.url %}
                            <a class="nav-link active " href="{{ entry.url | url }}">{{ entry.title }}</a>
                        {% else %}
                            <a class="nav-link" href="{{ entry.url | url }}">{{ entry.title }}</a>
                        {% endif %}
                    </li>
                {%- endfor %}
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item container me-0">
                    <div class="row w-200px ms-auto">
                        {% include "snippets/contactlogos.njk" %}
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>


